
import React from 'react';

const Header: React.FC = () => {
    // This component is used as a spacer to push content below the absolute-positioned TopRightControls.
    return (
        <header className="flex-shrink-0 h-16" />
    );
};

export default Header;
